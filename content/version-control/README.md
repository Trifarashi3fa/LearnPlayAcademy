# Curriculum Version Control

This folder will record curriculum package versions, approvals, effective dates, change summaries, and superseded records.

It is separate from Git history: Git records code changes, while these records will explain the educational content version and its approval lifecycle. No runtime version selection is implemented in Phase 3.